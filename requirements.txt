# ============================================================
# Speech Restoration Pipeline — requirements.txt
# ============================================================
# Install: pip install -r requirements.txt
#
# Core (required)
# ============================================================
numpy>=1.24.0
scipy>=1.10.0
torch>=2.0.0
torchaudio>=2.0.0

# ============================================================
# ASR — choose one (faster-whisper recommended)
# ============================================================
faster-whisper>=1.0.0
# openai-whisper>=20231117    # alternative

# ============================================================
# Audio enhancement (at least one recommended)
# ============================================================
noisereduce>=3.0.0           # lightweight, no GPU needed
# speechbrain>=1.0.0         # MetricGAN+ (heavier, better quality)
# denoiser>=0.1.5            # facebook/denoiser DNS48

# ============================================================
# VAD (at least one recommended)
# ============================================================
# silero-vad is loaded via torch.hub — no pip install needed
webrtcvad-wheels>=2.0.14     # cross-platform webrtcvad

# ============================================================
# Optional: Speaker Diarization
# ============================================================
# pyannote.audio>=3.1.0      # requires HuggingFace token
# pyannote.core>=5.0.0

# ============================================================
# Optional: LLM postprocessing
# ============================================================
# openai>=1.0.0              # for GPT-4o postprocessing
# anthropic>=0.30.0          # for Claude postprocessing

# ============================================================
# Optional: sklearn for simple diarization fallback
# ============================================================
scikit-learn>=1.3.0

# ============================================================
# Utilities
# ============================================================
requests>=2.31.0
